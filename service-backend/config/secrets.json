{
  "_comment": "Keys = env var names in the pod. Values = GitHub repo secret names (no real values here). Create with: gh secret set <NAME> --repo nurixlabs/team-ppo-nahi-milega --body '<actual-value>'",
  "ANTHROPIC_API_KEY":   "anthropic_api_key",
  "GOOGLE_CLIENT_ID":    "google_client_id",
  "GOOGLE_CLIENT_SECRET":"google_client_secret",
  "GOOGLE_REDIRECT_URI": "google_redirect_uri",
  "JWT_SECRET":          "jwt_secret"
}
