{
  "_comment": "Keys = env var names in the pod. Values = GitHub repo secret names (no real values here). Create with: gh secret set <value> --repo nurixlabs/team-ppo-nahi-milega --body '<actual-value>'",
  "ANTHROPIC_API_KEY": "anthropic_api_key"
}
